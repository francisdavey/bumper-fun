source "https://rubygems.org"

# This is the same gem set GitHub Pages builds with, so what works
# locally will work when you push.
gem "github-pages", group: :jekyll_plugins

# Needed for `jekyll serve` on modern Ruby versions.
gem "webrick"
